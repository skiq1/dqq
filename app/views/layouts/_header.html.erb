<nav class="navbar navbar-dark bg-dark navbar-expand-lg">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <a class="navbar-brand" href="#">teleinf</a>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", posts_path, class: "nav-link active"%>
        </li>
        <li class="nav-item">
          <%= link_to "URLs", redirect_posts_path, class: "nav-link active"%>
        </li>
        <%# <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true">Disabled</a>
        </li> %>
      </ul>

      <% if current_user %>
        <%= render  "layouts/button" %>
      <% elsif %>
        <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-primary me-2" %>
      <% end %>

      <%= search_form_for @q, class: "d-flex" do |f| %>
      <%= f.search_field :title_or_description_or_slug_or_files_blobs_filename_cont, class: "form-control me-2", placeholder: "Search"%>
        <%= f.submit "Search", class: "btn btn-outline-success" %>
      <% end %>
    </div>
  </div>

</nav>
